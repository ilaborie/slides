(() => {
    const config = {
        minCapacity: 1,
        maxCapacity: 12,
        url: "/api/solve",
        debug: false
    };

    const host = document.querySelector("#demo article");

    // TODO
})();

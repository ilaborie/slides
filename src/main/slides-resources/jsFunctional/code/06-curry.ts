// type: (number, number) => number
const mult = (a, b) => a * b;

// idea: identity = mult(1, _)

// double = mult(2, _)

// triple = mult(3, _)

// [identity, double, triple]
//     .map(fun => fun(42))
//     .forEach(x => console.log(x));